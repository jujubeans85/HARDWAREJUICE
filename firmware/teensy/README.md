# Teensy Firmware

Parked for later.

No DIY hardware build until the first 8 software/Maschine controls prove useful.

Future role:

- USB-MIDI controller
- encoders mapped to namespace CC values
- optional RGB feedback

First firmware target later:

```text
8 encoders → CC21-28 → same namespace as Maschine
```

Do not create a second control language.
